
#user  nobody;
worker_processes  1;

#error_log  logs/error.log;
#error_log  logs/error.log  notice;
#error_log  logs/error.log  info;

#pid        logs/nginx.pid;


events {
    worker_connections  1024;
}


http {
    include       mime.types;
    default_type  application/octet-stream;

    #log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
    #                  '$status $body_bytes_sent "$http_referer" '
    #                  '"$http_user_agent" "$http_x_forwarded_for"';

    #access_log  logs/access.log  main;

    sendfile        on;
    #tcp_nopush     on;

    #keepalive_timeout  0;
    keepalive_timeout  65;

    #gzip  on;
    
    upstream backend {
    	server localhost:7891;
    }
    # 
    server {
        listen       80;
        server_name  localhost;

        #charset koi8-r;

        #access_log  logs/host.access.log  main;

        location / {
            root   html;
            index  index.html index.htm;
        }
	

		location /api/v1/ws/ {
			# rewrite掉多余的路径，(\d+)是匹配url上的userId
			rewrite ^/api/v1/ws/(\d+)/(.*)$ /$2 break;
			set $backend '';
			# docker 内嵌 DNS：变量 proxy_pass 需在运行期解析服务名
			resolver 127.0.0.11 valid=10s ipv6=off;
			# openresty lua 处理自定义的路由规则，这里直接hash到对应的实例上
			access_by_lua_block {
				-- 数组索引默认从1开始，将hash的最后一个实例放在数组1位置，这样取模后+1直接访问
				local servers = {"chatsimple_3:7891", "chatsimple_1:7891","chatsimple_2:7891"}  -- docker compose 服务名：容器内 127.0.0.1 是 openresty 自己，不是后端
				-- 路由键：X-User-Route 头（由 vite 代理等网关补充，$http_ 变量对大小写不敏感）
				local header_user_route = tonumber(ngx.var.http_x_user_route)
				-- 浏览器 WebSocket 无法携带 X-User-Route 自定义请求头，
				-- 回退从原始请求路径 /api/v1/ws/{userId}/... 解析（request_uri 不受上面 rewrite 影响）
				if not header_user_route then
					header_user_route = tonumber(ngx.var.request_uri:match("^/api/v1/ws/(%d+)/"))
				end
				if not header_user_route then
					ngx.log(ngx.ERR, "ws route: no X-User-Route header and no userId in path: ", ngx.var.request_uri)
					return ngx.exit(400)
				end

				local len_servers = #servers
				-- 数组索引默认从1开始,取模后+1
				ngx.var.backend = servers[(header_user_route % len_servers) + 1]
			}
			proxy_pass http://$backend;
			proxy_http_version 1.1;
			proxy_set_header Upgrade $http_upgrade;
			proxy_set_header Connection "upgrade";
			proxy_set_header Host $host;
			proxy_set_header X-Real-IP $remote_addr;
			proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
			proxy_set_header X-Forwarded-Proto $scheme;
			proxy_set_header X-User-Route $http_x_user_route;
			
			proxy_read_timeout 86400s;
			proxy_send_timeout 86400s;
		}

        #error_page  404              /404.html;

        # redirect server error pages to the static page /50x.html
        #
        error_page   500 502 503 504  /50x.html;
        location = /50x.html {
            root   html;
        }

        # proxy the PHP scripts to Apache listening on 127.0.0.1:80
        #
        #location ~ \.php$ {
        #    proxy_pass   http://127.0.0.1;
        #}

        # pass the PHP scripts to FastCGI server listening on 127.0.0.1:9000
        #
        #location ~ \.php$ {
        #    root           html;
        #    fastcgi_pass   127.0.0.1:9000;
        #    fastcgi_index  index.php;
        #    fastcgi_param  SCRIPT_FILENAME  /scripts$fastcgi_script_name;
        #    include        fastcgi_params;
        #}

        # deny access to .htaccess files, if Apache's document root
        # concurs with nginx's one
        #
        #location ~ /\.ht {
        #    deny  all;
        #}
    }


    # another virtual host using mix of IP-, name-, and port-based configuration
    #
    #server {
    #    listen       8000;
    #    listen       somename:8080;
    #    server_name  somename  alias  another.alias;

    #    location / {
    #        root   html;
    #        index  index.html index.htm;
    #    }
    #}


    # HTTPS server
    #
    #server {
    #    listen       443 ssl;
    #    server_name  localhost;

    #    ssl_certificate      cert.pem;
    #    ssl_certificate_key  cert.key;

    #    ssl_session_cache    shared:SSL:1m;
    #    ssl_session_timeout  5m;

    #    ssl_ciphers  HIGH:!aNULL:!MD5;
    #    ssl_prefer_server_ciphers  on;

    #    location / {
    #        root   html;
    #        index  index.html index.htm;
    #    }
    #}

}
