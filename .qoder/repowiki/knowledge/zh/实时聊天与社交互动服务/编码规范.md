- 统一响应封装：所有Controller接口返回`Payload<T>`对象，包含状态码、消息及数据体。
- 上下文传递：使用`ContextUtil`结合`TransmittableThreadLocal`在请求链路中传递当前用户ID、用户名等信息。
- 数据对象规范：严格区分持久层对象(PO)、数据传输对象(DTO)和视图对象(VO)，并通过`BeanConvert`进行转换。
- 异常处理：业务异常统一抛出`BusinessException`，由`ExceptionAspectHandler`全局捕获并转换为标准错误响应。